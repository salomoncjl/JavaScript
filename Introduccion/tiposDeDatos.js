// Booleano
const usuarioConectado = false;
const mayorQue = 10 > 2;

// Arrays - Arreglos
const arreglo = [1, 10, 5]

// Objetos

const persona = {
    nombre: 'Carlos',
    edad: 22,
    carro: {
        marca: '...',
        color: 'negro',
        modelo: 2023
    }
}

// Funcion

function saludar(){
    console.log('hola')
}

// Null
// Normalmente lo usamos cuando queremso especificar que un valor sea nulo.
const miVariable = null;

// Undefined
// Se usa para indicarnos que un valor no esta definido.
const miVariable2 = undefined;